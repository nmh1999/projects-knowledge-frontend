import { Component, inject, input } from '@angular/core';import { ApiInfo } from '../../../core/models/knowledge-answer.model';import { LanguageService } from '../../../core/services/language.service';
@Component({selector:'app-api-list',standalone:true,template:`<section class="detail-card"><h3>{{language.t('apis')}} <span>{{items().length}}</span></h3>@for(item of items();track item.method+item.path){<article><b [attr.data-method]="item.method">{{item.method}}</b><div><code>{{item.path}}</code><p>{{item.controller}} · {{item.methodName}}</p></div></article>}</section>`,styles:[`.detail-card{height:100%;padding:20px}h3{font-size:15px;margin:0 0 14px}h3 span{font-size:9px;background:#eaf0f5;padding:4px 7px;border-radius:9px;margin-inline-start:4px}article{display:flex;align-items:center;gap:11px;padding:11px 0;border-bottom:1px solid #edf1f4}article:last-child{border:0}b{font-size:9px;padding:5px 7px;border-radius:5px;background:#e5f7ef;color:#187650}b[data-method=POST]{background:#e8f0ff;color:#416fc2}b[data-method=DELETE]{background:#ffebed;color:#b94655}code{font-size:11px;font-weight:700;color:var(--ink);overflow-wrap:anywhere}p{font-size:10px;color:var(--muted);margin:4px 0 0}`]})export class ApiListComponent{readonly language=inject(LanguageService);readonly items=input<ApiInfo[]>([]);}

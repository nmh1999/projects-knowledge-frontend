import { Component, inject, input } from '@angular/core';import { IntegrationInfo } from '../../../core/models/knowledge-answer.model';import { LanguageService } from '../../../core/services/language.service';
@Component({selector:'app-integrations',standalone:true,template:`<section class="detail-card"><h3>{{language.t('integrations')}} <span>{{items().length}}</span></h3>@for(item of items();track item.name){<article><span>⌁</span><div><strong>{{item.name}}</strong><p>{{item.purpose}}</p></div></article>}</section>`,styles:[`.detail-card{height:100%;padding:20px}h3{font-size:15px;margin:0 0 14px}h3 span{font-size:9px;background:#eaf0f5;padding:4px 7px;border-radius:9px;margin-inline-start:4px}article{display:flex;gap:11px;padding:10px 0;border-bottom:1px solid #edf1f4}article:last-child{border:0}article>span{width:31px;height:31px;display:grid;place-items:center;border-radius:8px;background:#f2eafb;color:#7a42a6;flex:none}strong{font-size:11px}p{font-size:10px;color:var(--muted);margin:4px 0 0;line-height:1.5}`]})export class IntegrationsComponent{readonly language=inject(LanguageService);readonly items=input<IntegrationInfo[]>([]);}

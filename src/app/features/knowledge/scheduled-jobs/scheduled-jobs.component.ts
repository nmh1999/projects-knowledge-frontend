import { Component, inject, input } from '@angular/core';import { ScheduledJobInfo } from '../../../core/models/knowledge-answer.model';import { LanguageService } from '../../../core/services/language.service';
@Component({selector:'app-scheduled-jobs',standalone:true,template:`<section class="detail-card"><h3>{{language.t('scheduledJobsTitle')}} <span>{{items().length}}</span></h3>@for(item of items();track item.name){<article><span>◷</span><div><strong>{{item.name}}</strong><code>{{item.schedule}}</code></div></article>}</section>`,styles:[`.detail-card{height:100%;padding:20px}h3{font-size:15px;margin:0 0 14px}h3 span{font-size:9px;background:#eaf0f5;padding:4px 7px;border-radius:9px;margin-inline-start:4px}article{display:flex;gap:11px;padding:10px 0;border-bottom:1px solid #edf1f4}article:last-child{border:0}article>span{width:31px;height:31px;display:grid;place-items:center;border-radius:8px;background:#fff4df;color:#a56a16;flex:none}strong,code{display:block}strong{font-size:11px}code{font-size:9px;color:var(--muted);margin-top:4px;overflow-wrap:anywhere}`]})export class ScheduledJobsComponent{readonly language=inject(LanguageService);readonly items=input<ScheduledJobInfo[]>([]);}
